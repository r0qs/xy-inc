akka {
  loglevel = INFO
}

spray.can.server {
  request-timeout = 1s
}

app {
  interface = "localhost"
  port      = 8080
}

database {
  url = "jdbc:sqlite:./pois.sqlite"
  driver = org.sqlite.JDBC
  user = ""
  password = null
}
